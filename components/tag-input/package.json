{
  "name": "tag-input",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "esbuild": "^0.11.5",
    "preact": "^10.5.13",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "scripts": {
    "build": "esbuild index.jsx --bundle --minify --sourcemap --loader:.js=jsx --outfile=dist/index.js",
    "preserve": "yarn build",
    "serve": "npx http-server"
  }
}
